/**
 * My Pi Car - Systemkonfiguration
 * Ändra CAR_URL när du startar en ny ngrok-tunnel.
 */
const CONFIG = {
    // Din ngrok-adress från screenshot (utan https://)
    CAR_URL: "nonconterminal-maeve-subopaquely.ngrok-free.dev",
    
    // Namn på bilen som visas i kontrollpanelen
    CAR_NAME: "Imam Hussein FPV",
    
    // Automatisk återanslutning (ms)
    RECONNECT_INTERVAL: 5000
};
